@model RaceDirector.Models.ViewModels.DriverVM

@{
    ViewData["Title"] = @Html.DisplayFor(model => model.FullName);
}

<h2>@Html.DisplayFor(model => model.FullName)</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            Возраст
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AgeString) (@Html.DisplayFor(model => model.DateOfBirth))
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Редактировать", "Edit", new { id = Model.Id }) |
    <a asp-action="Index">Назад к списку пилотов</a>
</div>
